{
  "mcpServers": {
    "agentmemory": {
      "command": "bash",
      "args": [
        "-lc",
        "if [ -n \"${CLAUDE_PLUGIN_ROOT:-}\" ] && [ -x \"$CLAUDE_PLUGIN_ROOT/scripts/run-mcp.sh\" ]; then exec \"$CLAUDE_PLUGIN_ROOT/scripts/run-mcp.sh\"; fi; for f in \"$HOME/.codex/plugins/cache/agentmemory/agentmemory\"/*/scripts/run-mcp.sh \"$PWD/plugin/scripts/run-mcp.sh\"; do [ -x \"$f\" ] && exec \"$f\"; done; exec npx -y @agentmemory/mcp"
      ]
    }
  }
}
